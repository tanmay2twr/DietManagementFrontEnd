import { ChallengerRegistration } from './challenger-registration';

describe('ChallengerRegistration', () => {
  it('should create an instance', () => {
    expect(new ChallengerRegistration()).toBeTruthy();
  });
});
